

## Eliminar subtítulo "Mis Viajes Registrados"

Eliminar el encabezado "Mis Viajes Registrados" del tab de viajes en `src/components/Dashboard.tsx`, ya que es redundante con el tab mismo.

### Cambio

**`src/components/Dashboard.tsx`**: Buscar y eliminar el elemento que renderiza "Mis Viajes Registrados" (probablemente un `<h3>` o similar) dentro del contenido del tab de viajes.

